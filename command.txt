git init 

following steps are to be used if any changes are doe in the file:
git add .
git commit -m "message"
git push origin branchname

for switching branch:
git checkout branchname

to create new branch:
git branch newbranchname
